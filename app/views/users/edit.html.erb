<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
       <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this recipe from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="field">
    <%= f.label "ESF_Nickiniz" %><br>
    <%= f.text_field :nick %>
  </div>


  <div class="actions">
    <%= f.submit "Nick'i onayla" %>
  </div>

<% end %>
